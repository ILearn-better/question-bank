# 专属题库工具

个人教师自用的数学题库工具。当前已实现：

- **试卷录入** —— PDF / Word 视觉分割（点两条分界线框选题目，不做文字识别），答案可单独配图；
  已录的题也能事后补答案
- **题库与出卷** —— 多条件筛题（体系 / 知识点 / 标签 / 题型 / 难度 / 有无图）、组卷调序、导出 HTML / Word / PDF
- **学生管理** —— 学生档案、能力维度与雷达图
- **课表与课时费** —— 课时排期、课时费结算、家长反馈
- **笔记** —— Markdown 正文 + 公式实时渲染 + Ctrl+V 贴图 + 板书笔画（红黄蓝 / 橡皮 / 粗细 / 撤销）、
  全屏专注模式；可导出 Word / PDF（**公式转成 Word 原生公式**，导出后还能双击编辑）

> 文档都在 `docs/`：
>
> - [技术方案设计.md](docs/技术方案设计.md) —— 技术选型、系统架构、数据库设计、开发计划
> - [模块化教学管理系统-调研与开发文档.md](docs/模块化教学管理系统-调研与开发文档.md) —— 早期调研
> - [版面识别方案调研.md](docs/版面识别方案调研.md) —— 版面分析 / 题目切分选型对比与成本（与 V1.5 相关）

## 开发路线

| 阶段 | 内容 | 状态 |
|------|------|------|
| MVP-1 | 知识点树 + PDF/Word 分割录入 | ✅ 已完成 |
| M0–M3 | 共享内核、题库多体系化、学生管理、课表与课时费 | ✅ 已完成 |
| MVP-2 | 题库浏览 / 筛选（体系、知识点、题型、难度） | ✅ 已完成（出卷页 + `GET /api/questions/search`） |
| MVP-3 | 筛选组卷 + 导出 HTML / Word / PDF | ✅ 已完成（`/papers`）；「作业布置」尚未做 |
| MVP-4 | 笔记（Markdown + 公式 + 插图 + 板书，导出 Word / PDF） | ✅ 已完成（`/notes`） |
| V1.5 | OCR 通道（扫描版自动识别） | 待开发（扫描版目前走纯截图模式，可用）；选型见 [docs/版面识别方案调研.md](docs/版面识别方案调研.md) |
| V2 | 智能组卷、错题本、薄弱点推题 | 待开发（`resources` 表已建好，暂无接口） |

## 项目结构

```
question-bank/
├── backend/
│   ├── main.py                 # 只做装配：建 app、挂路由、挂静态资源
│   ├── app/                    # 业务代码全在这里
│   │   ├── config.py           # 路径与开关集中处（环境变量统一 SHIKE_ 前缀）
│   │   ├── db.py               # 全项目唯一 SQLite 接入点（WAL / 外键 PRAGMA）
│   │   ├── models.py  schemas.py  migrate.py  seed.py
│   │   ├── adapters/           # pdf.py（唯一 import pymupdf）/ office.py（Word COM，唯一 import win32com）
│   │   │                       # notes_math.py + katex_mathml.js（公式 → Word 原生公式 OMML）
│   │   ├── routers/            # dashboard / students / lessons / feedbacks / curriculum / questions
│   │   │                       # papers / documents / notes / uploads / system
│   │   └── services/           # billing（课时费）/ mastery（掌握度）/ paper_export（出卷排版）
│   │                           # images（按魔数校验图片）/ notes_export（笔记导出 Word/PDF）
│   ├── alembic/versions/       # 0001 … 0009
│   ├── doc_parser.py           # Word 解析（documents.py 延迟导入，仍在用）
│   ├── requirements.txt
│   ├── shike.db                # SQLite 主库（自动创建，已 gitignore）
│   └── data/                   # 本机运行时数据，整目录已 gitignore
│       ├── uploads/            # 原件 / pages 页面渲染缓存 / crops 框选截图 / converted Word 转出的 PDF
│       └── backups/            # 数据库备份，保留最近 10 份（SHIKE_BACKUP_KEEP 可改）
├── frontend/
│   ├── index.html + src/       # 工作台 SPA（Vue3 零构建，走 import map → vendor/）
│   ├── entry.html              # 题库录题页（单文件，Element Plus + KaTeX）
│   └── app.css  vendor/        # 依赖**全部本地化**（Vue / Element Plus / KaTeX / marked / DOMPurify）
│                               # 不引任何 CDN：实测本机冷启动拉 unpkg 要 29s 且经常超时
├── docs/
│   ├── 技术方案设计.md          # 技术选型、系统架构、数据库设计、开发计划
│   ├── 模块化教学管理系统-调研与开发文档.md
│   └── 版面识别方案调研.md      # 版面分析 / 题目切分选型对比与成本（与 V1.5 相关）
├── data/
│   └── math-knowledge-tree.json   # 高中数学知识点树（13 板块，导入库后共 119 个节点）
└── samples/                    # 样例试卷（正常 PDF/docx + 扫描版）
```

### 遗留文件

以下是重构前的产物，**已不被应用引用**，保留与否待定：

- `backend/pdf_pages.py` —— 页面渲染 / 文字行 / 裁剪，已被 `app/adapters/pdf.py` 取代
- `backend/gen_samples.py`、`backend/gen_scanned_sample.py` —— 生成样例试卷
- `backend/test_visual.py`、`backend/test_scanned.py` —— 对着运行中的服务手工跑的端到端脚本
- `backend/app.db` —— 重构前的空库（0 行），现用 `shike.db`
- `backend/uploads/` —— 空目录，已并入 `backend/data/uploads/`
- `frontend/_debug.html` —— 调试残留

## 启动方式

```bash
cd question-bank/backend
pip install -r requirements.txt
python -m uvicorn main:app --host 127.0.0.1 --port 8000 --reload
```

浏览器打开 http://127.0.0.1:8000

## 数据说明

- 所有题目、文档记录存在 `backend/shike.db`（SQLite，WAL 模式）。
  要手工备份这个文件的话，**先停服务再拷** —— 库开着 WAL，最近的写入可能还在
  `shike.db-wal` 里没合并回主库，只拷主文件会拷走一份缺一块的数据
- 数据库结构需要变更时，启动会自动备份到 `backend/data/backups/`，只保留最近 10 份
  （`SHIKE_BACKUP_KEEP` 可改）
- 上传的原文件、页面渲染缓存、裁剪截图存在 `backend/data/uploads/`
- 知识点树在 `data/math-knowledge-tree.json`，接口为 `GET /api/tree`

## 使用流程

录一道题都是两步：**① 在左侧圈出题目 →「确认题目，去选答案」→ ② 圈出答案 →「保存到题库」**。
答案也可以不圈，直接「跳过答案，直接保存」。左上方两个胶囊可在两步之间来回切。

### 标签与知识点

录题时可以给题目打**标签**（自由输入、回车确认）与**知识点**（可多选）。两者都会成为出卷页的筛选条件：

- **标签**：多选，**任一命中**。括号里是题数。
- **知识点**：选项把**所有体系**的节点都列在一起（按体系分组），也可以**自己敲**
  —— 目前只有国内高中数学有内置知识树（119 个节点），其他体系请直接手填。
  按知识点筛会连**子节点**一起算，所以选「平面向量」能找到挂在它下面的题。

### 给已录的题补答案（顶部「补答案」模式）

顶部切到「补答案」→ 用关键字搜一道已录的题（或勾「只看还没答案的」）→ 补答案 →「保存答案」。
答案图的三种来源：

1. **📁 从电脑选图片** —— 本机任意图片
2. **🖼 从已有截图选** —— 从库里框选过的截图里挑
3. **上传/切到答案文档后在页面上框选** —— 左边仍然是行标记/框选，圈出来会自动写进**答案图**
   （不会碰到题干与题干图，因为这一步锁定在「第②步」）

保存走 `PATCH`，只改答案相关字段，题干与题干图不受影响。

### Word：行标记（主流程，不做文字识别）

上传 .docx 后，服务端会调用**本机 Word** 把它导出一份 PDF 当页面视图用 —— 首次进入要等几秒，
转换失败（或没装 Word）会自动切到下面的「内容块」模式。

1. 工具选「📝 行标记」：在题目**上方点一下**放起点线，在题目**下方点一下**放终点线
2. 两条线之间（整幅宽，可跨页）就是这道题，自动截成**原貌图**。
   这里刻意**不识别文字** —— 公式、图形、排版全部保真，也不依赖文档有没有文字层
3. 「确认题目，去选答案」→ 第 ② 步在页面上圈出答案图 →「保存到题库」

两条线可以拖动微调、点 × 删除、放完再点即重新标记；勾「吸附到行间空隙」会让线贴到行与行之间。
需要按矩形精确框选时，把工具切到「📷 图片选择」。

### Word：内容块模式（转换不可用时的退路）

1. 顶部切到「内容块」视图，点击一个块设**起点**，再点一个设**终点**
2. 右侧生成预览（KaTeX 渲染 `$...$` 公式），填标签保存

### PDF：页面分割（推荐）

1. 上传 PDF 后自动进入「页面分割」视图，系统按题号**自动放好分界线**（可点「自动识别题号」重新识别）
2. ✂️ 分界线工具：点击页面插入分界线（吸附行间隙；点已有线附近删除；拖动红线微调）
3. 点击段落左侧「页-序号」标签选中该段（**可多选、跨页**），右侧生成题目文本
4. 可点「📷 附加到题目」把选中区域截为高清图随题目入库（公式保真）
5. 📷 框选截图工具：拖矩形罩住整道题，生成图片题（适合图形题/公式密集题）
6. 填题型 / 难度 / 知识点后「确认题目，去选答案」→ 圈答案 → 保存

### 扫描版 PDF：纯截图模式

1. 上传无文字层的扫描版 PDF（手机拍照转的 PDF 也算）不再被拒绝，自动标记「扫描版」
2. 跳过文字解析，直接进入「📷 截图模式」：在页面上拖矩形框住一道题，自动截取 3 倍分辨率高清图入库
3. 待 OCR 通道接入后，扫描版将支持自动识别文字

通用：已录入列表支持删除和导出 JSON；顶部可切换历史文档继续分割。

## 已知限制（后续迭代）

- PDF 文字抽取对复杂数学公式保真度有限 → 用「附加原貌截图」兜底，人工改 LaTeX
- 早期上传的旧文档没有保留原文件，页面分割会提示重新上传
- 扫描版 PDF 暂无 OCR，只能截图录入（文字自动识别待 V1.5）
- docx 中表格与正文的先后顺序不保持（表格追加在末尾）
- `.doc` 旧格式不支持，需另存为 `.docx`
